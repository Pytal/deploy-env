{
  "name": "deploy-env",
  "version": "1.0.2",
  "main": "src/mod.ts",
  "bin": {
    "dpl-env": "src/cli.ts",
    "deploy-env": "src/cli.ts"
  },
  "scripts": {
    "prepublishOnly": "pnpm run build",
    "build": "rm -rf lib && tsc",
    "postbuild": "pnpm run shebang && pnpm run terse",
    "shebang": "node --experimental-top-level-await scripts/shebang.mjs lib/cli.js",
    "terse": "ts-node scripts/terse.ts lib/"
  },
  "dependencies": {
    "env-cmd": "^10.1.0"
  },
  "devDependencies": {
    "@types/node": "^14.0.18",
    "terser": "^4.8.0",
    "ts-node": "^8.10.2",
    "typescript": "^3.9.6"
  },
  "publishConfig": {
    "main": "lib/mod.js",
    "bin": {
      "dpl-env": "lib/cli.js",
      "deploy-env": "lib/cli.js"
    }
  },
  "files": [
    "lib"
  ],
  "engines": {
    "node": ">=10"
  },
  "repository": "github:Pytal/deploy-env"
}
